<%# locals: (attachments:, caption:, downloadable_from: nil) %>
<%# Un inventaire, dont seules les pièces reçues d'un dépôt se téléchargent : `downloadable_from`
    est la démarche, donnée pour le dépôt seulement, les pièces d'un événement n'ayant pas
    d'adresse. La caption, exigée par le RGAA, est masquée : la section qui appelle porte déjà
    le titre. %>
<div class="fr-table fr-table--bordered fr-table--no-caption">
  <div class="fr-table__wrapper">
    <div class="fr-table__container">
      <div class="fr-table__content">
        <table>
          <caption><%= caption %></caption>
          <thead>
            <tr>
              <th scope="col"><%= t("portail.deliveries.attachment_fields.filename") %></th>
              <th scope="col"><%= t("portail.deliveries.attachment_fields.kind") %></th>
              <th scope="col"><%= t("portail.deliveries.attachment_fields.byte_size") %></th>
              <th scope="col"><%= t("portail.deliveries.attachment_fields.state") %></th>
              <% if downloadable_from %>
                <th scope="col"><%= t("portail.deliveries.attachment_fields.download") %></th>
              <% end %>
            </tr>
          </thead>
          <tbody>
            <% attachments.each do |attachment| %>
              <tr>
                <td><%= attachment.filename %></td>
                <td><%= attachment.kind %></td>
                <td><%= delivery_attachment_size(attachment) %></td>
                <td><%= delivery_attachment_state(attachment) %></td>
                <% if downloadable_from %>
                  <td>
                    <% if attachment.state_received? %>
                      <%# `download` : le navigateur reçoit le fichier lui-même, Turbo n'intercepte pas.
                          Le nom accessible porte la pièce : un même intitulé par ligne ne suffit pas au RGAA. %>
                      <%= link_to t("portail.deliveries.attachments.download"),
                            demarche_piece_path(downloadable_from.id, attachment.id),
                            class: "fr-btn fr-btn--sm fr-btn--secondary fr-icon-download-line fr-btn--icon-left",
                            aria: {label: t("portail.deliveries.attachments.download_named", filename: attachment.filename)},
                            download: true %>
                    <% end %>
                  </td>
                <% end %>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>
