<% content_for :title, t(".title", number: @delivery.number) %>

<h1><%= t(".heading", number: @delivery.number) %></h1>

<dl class="fr-mb-4w">
  <dt><%= t("portail.deliveries.fields.state") %></dt>
  <dd><%= delivery_state(@delivery) %></dd>

  <dt><%= t("portail.deliveries.fields.data_stream") %></dt>
  <dd><%= @delivery.data_stream.code %></dd>

  <dt><%= t("portail.deliveries.fields.transmitted_at") %></dt>
  <dd><%= delivery_transmitted_at(@delivery) %></dd>

  <dt><%= t("portail.deliveries.fields.updated_at") %></dt>
  <dd><%= delivery_updated_at(@delivery) %></dd>

  <%# Le demandeur est absent de la liste servie en amont, mais présent au détail. %>
  <dt><%= t("portail.deliveries.fields.applicant") %></dt>
  <dd><%= delivery_applicant(@delivery) %></dd>
</dl>

<%= link_to t(".back"), demarches_path, class: "fr-link fr-icon-arrow-left-line fr-link--icon-left" %>
