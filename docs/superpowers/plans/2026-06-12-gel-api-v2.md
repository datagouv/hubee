# Gel API V2 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Geler proprement l'ébauche d'API V2 (routes, request specs) sans supprimer de code, pour permettre une reprise ultérieure.

**Architecture:** Commenter le bloc `namespace :api` dans `config/routes.rb` avec un commentaire de gel explicite. Exclure les request specs `spec/requests/api/` du run RSpec par défaut via `--exclude-pattern` dans `.rspec`. Documenter le statut dans le README.

**Tech Stack:** Rails 8.1, RSpec, `bin/ci`

---

### Task 1 — Commenter le bloc `namespace :api` dans `config/routes.rb`

**Fichiers :**
- Modifier : `config/routes.rb`

- [ ] **Step 1 : Remplacer le contenu de `config/routes.rb`**

```ruby
Rails.application.routes.draw do
  get "up" => "rails/health#show", :as => :rails_health_check

  # =============================================================================
  # API V2 — GELÉE LE 2026-06-12
  #
  # L'API V2 est dépriorisée au profit du développement du portail V2
  # (repo datagouv/hubee), qui consomme l'API V1 via une gem cliente privée.
  #
  # Ces routes ne doivent PAS être réactivées pour corriger un bug :
  # le gel est intentionnel. La reprise du développement API V2 se fera
  # lorsque nécessaire, dans ce même repo.
  #
  # Contexte et décisions d'architecture (Notification vs Delivery,
  # can_read/can_write vs read_package/create_package, AASM vs boolean) :
  # voir "Registre des décisions" sur docs.numerique.gouv.fr
  # =============================================================================
  #
  # namespace :api do
  #   namespace :v1 do
  #     resources :organizations, only: %i[index show] do
  #       resources :subscriptions, only: %i[index]
  #     end
  #
  #     resources :data_streams do
  #       resources :subscriptions, only: %i[index create]
  #       resources :data_packages, only: %i[index create]
  #     end
  #
  #     resources :subscriptions, only: %i[show update destroy], param: :id
  #
  #     resources :data_packages, only: %i[index show destroy], param: :id do
  #       resource :transmission, only: %i[create]
  #       resources :subscriptions, only: %i[index], controller: "data_packages/subscriptions"
  #     end
  #   end
  # end
end
```

- [ ] **Step 2 : Vérifier qu'aucune route `api/v1` n'est exposée**

```bash
bin/rails routes 2>/dev/null | grep api
```

Résultat attendu : aucune ligne.

---

### Task 2 — Exclure les request specs API du run par défaut

**Fichiers :**
- Modifier : `.rspec`

- [ ] **Step 1 : Ajouter `--exclude-pattern` dans `.rspec`**

```
--require spec_helper
--format documentation
--color
--exclude-pattern "spec/requests/api/**/*_spec.rb"
```

- [ ] **Step 2 : Vérifier que les specs API sont exclues**

```bash
bundle exec rspec --dry-run 2>&1 | grep "requests/api"
```

Résultat attendu : aucune ligne (fichiers non listés).

- [ ] **Step 3 : Vérifier que les specs modèles/interactors tournent toujours**

```bash
bundle exec rspec --dry-run 2>&1 | grep -c "_spec.rb"
```

Résultat attendu : un nombre > 0 (les autres specs sont bien présentes).

---

### Task 3 — Documenter le statut API V2 dans le README

**Fichiers :**
- Modifier : `README.md` — insérer après la section `## 🏗️ Architecture` (ligne ~161)

- [ ] **Step 1 : Insérer la section après le bloc Architecture**

Ajouter après la liste de l'architecture (avant `## 📚 Documentation`) :

```markdown
## 🔒 Statut de l'API V2

L'ébauche d'API V2 présente dans ce repo (routes `api/v1`, 6 modèles, interactors de transmission) est **gelée**.

Le portail V2 (repo [`datagouv/hubee`](https://github.com/datagouv/hubee)) consomme l'API V1 via une gem cliente privée. La reprise du développement API V2 se fera ultérieurement, dans ce même repo.

> Les routes sont commentées dans `config/routes.rb` et les request specs exclues du run par défaut. Ne pas décommenter sans décision explicite de l'équipe — voir "Registre des décisions" sur docs.numerique.gouv.fr.
```

---

### Task 4 — Vérifier que `bin/ci` passe au vert

- [ ] **Step 1 : Lancer `bin/ci`**

```bash
bin/ci
```

Résultat attendu : `All checks passed! Coverage >= 80%. Ready for merge.`

Si la coverage chute sous 80 % suite à l'exclusion des request specs, ajuster le seuil dans `spec/rails_helper.rb` :

```ruby
minimum_coverage 75
```

(Vérifier d'abord le taux réel avec `COVERAGE=true bundle exec rspec --format progress` avant d'ajuster.)

---

### Task 5 — Commit

- [ ] Invoquer la skill `hubee-claude-plugin:commit`
